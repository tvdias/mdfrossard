{
  "name": "mdfrossard-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all build:app start:cloudflare",
    "start:eleventy": "eleventy --serve --port=4000",
    "start:cloudflare": "wrangler pages dev public --compatibility-date=2026-03-24 --port 4000 --show-interactive-dev-session=false",
    "build": "npm run build:app",
    "build:app": "eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "ejs": "^3.1.10",
    "js-yaml": "^4.1.0",
    "luxon": "^3.5.0",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "wrangler": "^4.6.0"
  }
}
